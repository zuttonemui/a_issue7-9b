<%= form_with(model: [book, book_comment], local: false) do |f| %>
  <%= f.text_area :comment, class: "form-control w-50 mb-3 comment-textarea" %>
  <%= f.submit "送信", class: "btn btn-sm btn-secondary" %>
<% end %>